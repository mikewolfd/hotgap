import { Card } from "@hotgap/design-system";

export const Titled = () => (
  <Card title="Oregon" subtitle="The roughest of all 51 places.">
    <p>Families here aren't fully clear until about $77,000 a year.</p>
  </Card>
);

export const Plain = () => (
  <Card>
    <p>A plain surface to group related content on a screen.</p>
  </Card>
);
