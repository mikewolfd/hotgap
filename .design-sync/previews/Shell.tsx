import { Shell, VerdictHeadline } from "@hotgap/design-system";

export const Screen = () => (
  <Shell>
    <p className="brand">HotGap</p>
    <VerdictHeadline tone="good">When you earn more, you keep more.</VerdictHeadline>
    <p className="hint">The page frame centers a screen's content to a readable column.</p>
  </Shell>
);
