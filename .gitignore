node_modules/
dist/
*.tsbuildinfo
.superpowers/
.env
# engine/ — the local PolicyEngine service's virtualenv and bytecode
__pycache__/
*.pyc
engine/.venv/

# Screenshots from browser-driven design work
.playwright-mcp/

# Subagent git worktrees
.claude/worktrees/

# worker/: local secrets for `wrangler dev`, and the Env + runtime types that
# `wrangler types` regenerates on every typecheck
.dev.vars
worker-configuration.d.ts

# wrangler dev state (local cache, logs)
.wrangler/
