// The places surface. Scaffold only: the page renders through design/tokens.css
// once its module lands (design/journalist.html is the brief).
import "../../../design/tokens.css";

document.querySelector<HTMLElement>("#app")!.textContent = "HotGap — places surface, not yet built.";
