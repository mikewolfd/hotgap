// The citizen surface. Scaffold only: the page renders through design/tokens.css
// once its module lands (design/citizen.html is the brief).
import "../../../design/tokens.css";

document.querySelector<HTMLElement>("#app")!.textContent = "HotGap — citizen surface, not yet built.";
