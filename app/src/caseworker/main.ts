// The caseworker surface. Scaffold only: the page renders through design/tokens.css
// once its module lands (design/caseworker.html is the brief).
import "../../../design/tokens.css";

document.querySelector<HTMLElement>("#app")!.textContent = "HotGap — caseworker surface, not yet built.";
