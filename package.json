{
  "name": "hotgap",
  "private": true,
  "license": "AGPL-3.0-only",
  "engines": { "node": ">=22" },
  "workspaces": ["shared", "worker", "app", "pipeline"],
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc -b shared worker app pipeline",
    "readability": "node scripts/readability.mjs",
    "contract": "RUN_CONTRACT=1 vitest run contract",
    "e2e": "playwright test --config app/playwright.config.ts",
    "pipeline": "tsx pipeline/src/run.ts"
  },
  "devDependencies": {
    "@playwright/test": "^1.61.1",
    "@types/node": "^22.20.1",
    "tsx": "^4.23.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
